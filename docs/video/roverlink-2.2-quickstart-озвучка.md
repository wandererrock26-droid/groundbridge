# RoverLink 2.2 — быстрый старт — сценарий озвучки

Хронометраж: **05:20** · реплик: **35** · слов: **462**

Таймкод — момент, когда реплика должна начать звучать. Текст в видео
уже показан подписью в этот же момент, так что попадание проверяется
глазами: строка на экране = строка в сценарии.

Если начитываешь сам — говори спокойно, тексты рассчитаны примерно на
14 знаков в секунду с паузой в конце. Если гонишь через синтез речи
(Яндекс SpeechKit, ElevenLabs) — бери файл `.srt`, там те же реплики
с готовыми таймкодами.

---

### `00:00` — RoverLink. Наземная станция управления беспилотной машиной на Raspberry Pi 5

*(заставка на экране — можно молчать или прочесть название главы)*

**`00:05`**  Панель открывается в браузере по адресу станции — порт 8080. Работает и по локальной сети, и через WiFi-точку самой станции.

**`00:15`**  Первое, что видно на входе — предупреждение. Панель управляет реальной машиной, всё сделанное здесь сразу уходит в каналы управления.

**`00:27`**  Пароль из коробки — roverlink. Он не секрет и нужен только для первого входа. Подсказка исчезнет, как только пароль сменят.

**`00:37`**  Вводим пароль и заходим.

**`00:48`**  Мы внутри. В шапке — название, версия прошивки станции и активный режим работы. Справа — состояние связи с панелью.

### `00:57` — Шаг 1. Режим работы. Станция умеет управлять двумя типами машин

*(заставка на экране — можно молчать или прочесть название главы)*

**`01:03`**  Режим выбирается один раз, под конкретную машину. Слева — машина без полётного контроллера, справа — с ним.

**`01:13`**  Без полётного контроллера: станция сама притворяется бортом для Mission Planner и отдаёт управление по CRSF на ESP32. Failsafe в этом режиме держит сама станция.

**`01:26`**  С полётным контроллером: станция становится прозрачным мостом. Mission Planner разговаривает с полётником напрямую через USB, а за failsafe отвечает сам полётник.

### `01:38` — Шаг 2. Подключить железо. Инструкция подстраивается под выбранный режим

*(заставка на экране — можно молчать или прочесть название главы)*

**`01:45`**  Блок «Как подключить» показывает инструкцию только для активного режима.

**`01:52`**  Номера пинов здесь не написаны вручную — они считаются из порта UART. Поменяешь порт или плату — поменяется и инструкция.

**`02:02`**  Ниже — гребёнка целиком: зелёный пин 7 для TXD, белый пин 6 — земля. Нечётные пины в нижнем ряду, счёт от пина 1.

### `02:14` — Шаг 3. Источник управления. Mission Planner по сети: связь и каналы — разные вещи

*(заставка на экране — можно молчать или прочесть название главы)*

**`02:20`**  Источник один — Mission Planner по сети. Джойстик подключается к компьютеру с ним, а не к станции.

**`02:29`**  Два индикатора, и их нельзя путать: слева связь с Mission Planner, справа — идут ли каналы. Связь есть, а каналов нет — значит машина стоит на failsafe, а в Mission Planner не включён джойстик-оверрайд.

**`02:46`**  Внизу — живые каналы от активного источника. Это первое, что стоит проверить: подвигай стиками и убедись, что каждый канал реагирует так, как ты ждёшь.

### `03:00` — Шаг 4. Подключить Mission Planner. UDP-клиент: адрес и порт станции

*(заставка на экране — можно молчать или прочесть название главы)*

**`03:07`**  В Mission Planner выбирается тип подключения UDPCl, затем по очереди вводятся адрес и порт. Поэтому здесь они разнесены по отдельным полям — с отдельными кнопками копирования.

**`03:20`**  Копируем адрес в локальной сети...

**`03:22`**  ...и порт. Кнопка подтверждает копирование галочкой.

**`03:25`**  Второе поле — адрес через ZeroTier. По нему станция доступна из любой точки, где есть интернет, без пробросов портов.

### `03:34` — Шаг 5. Выход управления. CRSF на ESP32, скорость переключается на лету

*(заставка на экране — можно молчать или прочесть название главы)*

**`03:40`**  Выход CRSF включается тумблером. Под ним подписан порт и физические пины платы — те же, что в инструкции по подключению.

**`03:49`**  Скорость зависит от того, куда идёт сигнал: на приёмник полётного контроллера — 420 тысяч бод, на TX-модуль в радиобэй — 400 тысяч.

**`03:52`**  Переключается мгновенно, без перезапуска сервиса.

### `03:59` — Шаг 6. Failsafe. Что машина делает, когда связь пропала

*(заставка на экране — можно молчать или прочесть название главы)*

**`04:07`**  Отдельная вкладка, и она выделена не просто так: это самая важная настройка перед первым выездом.

**`04:15`**  Два варианта. Удерживать положение — станция продолжает слать заданные значения каналов. Прекратить вещание — замолкает совсем, чтобы сработал собственный failsafe приёмника или полётника.

**`04:31`**  Значения задаются по каналам. Обязательно проверь их на земле, физически оборвав связь: газ в центре не для всякой машины означает «стоп».

### `04:42` — Шаг 7. Сменить пароль. Последнее, что нужно сделать до выезда

*(заставка на экране — можно молчать или прочесть название главы)*

**`04:51`**  Пароль из коробки знает каждый, кто читал документацию. Меняем его прямо здесь.

**`05:06`**  Готово. Хеш лежит на самой станции и переживает обновление проекта, а сессии на других устройствах разлогиниваются сразу.

### `05:15` — Станция готова. Режим выбран, железо подключено, Mission Planner на связи, failsafe проверен, пароль сменён

*(заставка на экране — можно молчать или прочесть название главы)*
